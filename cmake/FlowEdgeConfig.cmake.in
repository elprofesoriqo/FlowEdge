@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
find_dependency(Threads)
if(@FLOWEDGE_NEEDS_MDSPAN@)
  find_dependency(mdspan CONFIG)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/FlowEdgeTargets.cmake")

check_required_components(FlowEdge)
